apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sqlite-data
  namespace: prometeusz
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 10Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-data
  namespace: prometeusz
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 5Gi
